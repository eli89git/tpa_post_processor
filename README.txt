The post processor is converting the export TCN file (format4) from Vcarve to format that fit to the TPA CNC mechinesץ

Enviroment preperations:
1. install python
2. pip install PySimpleGUI
3. python tcn_converter.py


EXE file preperation:
1. pip install pyinstaller
2. pyinstaller --onefile your_script.py